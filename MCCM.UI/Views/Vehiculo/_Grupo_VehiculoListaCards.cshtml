@model IEnumerable<MCCM.Entidad.TMCCM_Grupo_Vehiculo>

@if (Model.Count() != 0)
{
    foreach (var item in Model)
    {
        @Html.Partial("_Grupo_VehiculoCard", item)
    }
}
else
{
    <div class="text-center">
        <p>No hay vehículos por mostrar</p>
    </div>
}