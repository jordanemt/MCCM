@model IEnumerable<MCCM.Entidad.TMCCM_Grupo>

@if (Model.Count() != 0)
{
    foreach (var item in Model)
    {
        @Html.Partial("_Card", item)
    }
}
else
{
    <div class="text-center">
        <p>No hay grupos por mostrar</p>
    </div>
}