@model IEnumerable<MCCM.Entidad.TMCCM_Gasto>

@if (Model.Count() != 0)
{
    foreach (var item in Model)
    {
        @Html.Partial("_Card", item)
    }
}
else {
    <div class="text-center">
        <p>No hay gastos por mostrar</p>
    </div>
}